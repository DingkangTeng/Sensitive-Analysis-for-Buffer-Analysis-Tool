            # group = len(countries)
            # middle = (group - 1) // 2
            # for i in range(len(xticks) // group):
            #     start = i * group
            #     for j in range(group):
            #         if j != middle:
            #             xticks[start + j] = "0"